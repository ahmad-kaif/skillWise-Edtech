{
  "name": "video-chat-server",
  "version": "1.0.0",
  "description": "Signaling server for video chat",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-deps": "npm install",
    "setup": "npm install && npm install dotenv",
    "postinstall": "echo 'Setup completed! Run npm start to start the server'"
  },
  "dependencies": {
    "express": "^4.21.2",
    "livekit-client": "^2.11.2",
    "livekit-server-sdk": "^2.12.0",
    "socket.io": "^4.7.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
